apiVersion: v1
kind: Service
metadata:
  labels:
    service: "app-{{ color }}"
  name: "app-{{ color }}"
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    service: "app-{{ color }}"
  type: LoadBalancer